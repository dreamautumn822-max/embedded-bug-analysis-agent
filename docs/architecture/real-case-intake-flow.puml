@startuml
title 真实故障 Case 受控接入流程

start
:从受控系统导出已关闭工单;
:两名标注者独立标注;
:独立仲裁者确认最终标签;
if (人工脱敏确认?) then (是)
  :使用带盐摘要替换工单编号;
  :稳定编号替换敏感字段;
  :删除标注人员身份;
  :生成生产脱敏 Case;
  if (契约和敏感扫描通过?) then (是)
    :写入仓库外或已忽略目录;
    :检查重复工单与跨集合泄漏;
    :执行规则链和模型评估;
    :分别报告合成与真实指标;
  else (否)
    :退回逐行脱敏复核;
  endif
else (否)
  :禁止导入;
endif
stop

@enduml
